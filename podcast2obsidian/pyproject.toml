[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "podcast2obsidian"
version = "0.1.0"
description = "Download podcasts, transcribe locally, enrich with LLM, save to Obsidian"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.9",
    "yt-dlp>=2024.0",
    "openai>=1.0",
    "jinja2>=3.0",
    "tomli-w>=1.0",
    "rich>=13.0",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
mlx = ["mlx-whisper>=0.4"]
cpu = ["faster-whisper>=1.0"]
bot = ["aiogram>=3.0", "aiosqlite>=0.20"]
dev = [
    "pytest>=8.0",
    "pytest-tmp-files>=0.0.2",
]

[project.scripts]
podcast2obsidian = "podcast2obsidian.cli:app"

[dependency-groups]
dev = [
    "ruff>=0.15.9",
]
